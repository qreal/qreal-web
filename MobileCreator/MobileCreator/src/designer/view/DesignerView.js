define(["require", "exports"], function(require, exports) {
    
    
    var DesignerView = (function () {
        function DesignerView(controller) {
            this.controller = controller;
        }
        return DesignerView;
    })();
    exports.DesignerView = DesignerView;    
})
