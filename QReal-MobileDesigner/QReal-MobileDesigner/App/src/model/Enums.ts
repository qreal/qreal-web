module Enums {

    export enum ControlType {
        Page,
        Header,
        Button,
        Input
    }

    export enum PropertyType {
        Text,
        Id,
        Inline,
        Corners,
        Mini,
        Theme,
        Title,
        Header
    }
} 

export = Enums;