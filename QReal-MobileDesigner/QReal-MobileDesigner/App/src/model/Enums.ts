module Enums {

    export enum ControlType {
        Page,
        Button,
        Input
    }

    export enum PropertyType {
        Text,
        Id,
        Inline,
        Corners,
        Mini,
        Theme,

        Title
    }
} 

export = Enums;